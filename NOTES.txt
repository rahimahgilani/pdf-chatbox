<!-- 1. App.jsx --> holds your routes to different pages 
2. Main.jsx will hold the app div 
3. add to run backend and frontend concurently
4. UseNavigate to make buttons interactive and navigate to other component pages. 
e.g <button className="download-pdf" onClick={()=>navigate("/chat-pdf")}>
const navigate = useNavigate() --> add before return
import --> import { useNavigate } from 'react-router-dom';


BACKEND 
1. made an app folder 
2. made a virtual env
    python -m venv venv 
    venv\Scripts\activate

STEP 1: EMBEDDINGS
Link: https://www.sbert.net/docs/sentence_transformer/usage/semantic_textual_similarity.html
1. import sentence transformer 
2. then load a model 
3. make an array of sentences 
4. encode the sentences into embeddings 
5. pass the embeddings to similarity for comparison 

STEP 2: PDF EXTRACTION 
Link: https://pymupdf.readthedocs.io/en/latest/the-basics.html
1. if we make an embedding of the entire pdf that may create one vector that blurs every topic together; chunking helps us make more focused embeddings to improve search precision. 
2. we using overlapping to ensure that information doesn't get severed/cut off at the chunk boundary (incomplete sentences)
3. we call RecursiveCharacterTextSplitter  to cut on natural boundaries (paras, sentences then words) to keep things semantically coherent

STEP 3: Chunking using the RecursiveCharacterTextSplitter
Link: https://docs.langchain.com/oss/python/integrations/splitters/index#text-structure-based
Link: https://reference.langchain.com/python/langchain-text-splitters/character/RecursiveCharacterTextSplitter
1. call text splitter - define chunk size and chunk overlap 
2. error I was facing - pass the txt file the content is written to and apply encoding texts = text_splitter.split_text(open("output.txt", "r", encoding="utf8").read())
3. keep pdf file in same folder as the chunking file.


BRANCHING 

1. git checkout main
2. git pull origin main          (get the latest)
3. git checkout -b module-2-pdf-chunking   (branch BEFORE coding)
4. ...write your code...
5. git add .
6. git commit -m "extract and chunk PDF text"
7. (repeat 4-6 as many times as you want while working)
8. git push -u origin module-2-pdf-chunking
9. git checkout main
10. git merge module-2-pdf-chunking
11. git push origin main